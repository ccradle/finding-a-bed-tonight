<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- JSON structured logging (default) -->
    <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>tenantId</includeMdcKeyName>
            <includeMdcKeyName>userId</includeMdcKeyName>
            <includeMdcKeyName>traceId</includeMdcKeyName>
            <includeMdcKeyName>spanId</includeMdcKeyName>
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <level>level</level>
                <logger>logger_name</logger>
                <message>message</message>
                <thread>thread_name</thread>
            </fieldNames>
        </encoder>
    </appender>

    <!-- Plain-text console appender for local development -->
    <appender name="PLAIN_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - [tenant=%X{tenantId}] [user=%X{userId}] [trace=%X{traceId}] %msg%n</pattern>
        </encoder>
    </appender>

    <!--
        Phase B D55 / Sam warroom W-B-5: rate-limit the
        "Audit event published without TenantContext bound" WARN.
        DuplicateMessageFilter drops same-message repeats across the JVM;
        without it, a runaway scheduled job could flood Loki with 10k
        identical WARN lines per minute and mask the real signal. The
        companion Micrometer counter (fabt.audit.system_insert.count) is
        unrate-limited so Prometheus still sees the true rate.

        allowedRepetitions=0 means every occurrence after the first within
        the cache window is suppressed. cacheSize=500 bounds memory.
    -->
    <turboFilter class="ch.qos.logback.classic.turbo.DuplicateMessageFilter">
        <allowedRepetitions>0</allowedRepetitions>
        <cacheSize>500</cacheSize>
    </turboFilter>

    <!-- Local profile: plain-text output -->
    <springProfile name="local">
        <root level="INFO">
            <appender-ref ref="PLAIN_CONSOLE" />
        </root>
    </springProfile>

    <!-- Default (non-local): JSON structured output -->
    <springProfile name="!local">
        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE" />
        </root>
    </springProfile>

</configuration>
